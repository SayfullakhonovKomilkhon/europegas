# 🚀 Быстрый старт: Заполнение базы данных

## 📦 Что у вас есть

1. **`supabase-seed-data.sql`** - SQL скрипт для добавления всех данных
2. **`CONTENT_DESCRIPTION.md`** - Подробное описание всего контента сайта
3. **`SUPABASE_SETUP.md`** - Инструкция по настройке Supabase

## 🎯 Как добавить данные в Supabase

### Шаг 1: Откройте Supabase Dashboard
Перейдите на https://supabase.com → Ваш проект

### Шаг 2: Откройте SQL Editor
В боковом меню выберите **SQL Editor**

### Шаг 3: Запустите скрипт
1. Нажмите **New Query**
2. Скопируйте содержимое файла **`supabase-seed-data.sql`**
3. Вставьте в редактор
4. Нажмите **Run** (или Ctrl+Enter)

### Шаг 4: Проверьте результат
После выполнения вы увидите сообщение:
```
✅ ===== Данные успешно добавлены! =====

📦 Добавлено:
   ✓ 4 категории продуктов
   ✓ 30 продуктов (10 ECU, 10 Rail Injectors, 10 Gas Reducers)
   ✓ 10 основных филиалов
```

## 📊 Что будет добавлено

### Категории (4 шт):
1. **ECU Control Units** - Блоки управления ECU
2. **Rail Injectors** - Рейловые инжекторы
3. **Gas Reducers** - Газовые редукторы
4. **Other** - Другие компоненты

### Продукты (30 шт):
#### ECU Control Units (10 шт) - $72 до $450
- ECU SET EG24.4 BASICO ⭐
- ECU SET EG48.4 AVANCE ⭐
- И ещё 8 продуктов...

#### Rail Injectors (10 шт) - $17 до $70
- RAILGAS 4 CYL. ⭐
- RAIL DAKOTA 4 CYL. ⭐
- И ещё 8 продуктов...

#### Gas Reducers (10 шт) - $45 до $130
- EG PREMO 150 ⭐
- И ещё 9 продуктов...

⭐ = Рекомендуемый (отображается на главной)

### Филиалы (10 основных):
- **Ташкент** - 5 филиалов
- **Самарканд** - 1 филиал
- **Бухара** - 1 филиал
- **Фергана** - 1 филиал
- **Андижан** - 1 филиал
- **Наманган** - 1 филиал

## 🔍 Проверка данных

### В Supabase Dashboard:
1. Перейдите в **Table Editor**
2. Проверьте таблицы:
   - `product_categories` - должно быть 4 записи
   - `products` - должно быть 30 записей
   - `branches` - должно быть 10 записей

### На сайте:
1. Откройте http://localhost:3000
2. Проверьте:
   - Главная страница → секция "Рекомендуемые продукты" (5 товаров)
   - /products → все 30 продуктов
   - Карта филиалов → 10 маркеров

## 🛠️ Дополнительные возможности

### Добавление больше филиалов:
Файл `branchesData.ts` содержит данные о 140+ филиалах.
Вы можете добавить их, создав дополнительные INSERT запросы по аналогии.

### Добавление отзывов:
Создайте INSERT запросы для таблицы `testimonials`:

```sql
INSERT INTO public.testimonials (customer_name, rating, comment, is_approved, is_featured) VALUES
('Иван Петров', 5.0, 'Отличное качество и сервис!', true, true),
('Алексей Смирнов', 4.8, 'Очень доволен установкой.', true, false);
```

### Добавление изображений продуктов:
1. Загрузите фото в Supabase Storage → `products` bucket
2. Обновите `image_url` в таблице `products`

```sql
UPDATE products 
SET image_url = 'https://your-project.supabase.co/storage/v1/object/public/products/ecu-eg24-4.jpg'
WHERE slug = 'ecu-eg24-4-basico';
```

## 📝 Полезные SQL запросы

### Посмотреть все продукты:
```sql
SELECT name, price, category_id, is_featured 
FROM products 
ORDER BY price;
```

### Посмотреть рекомендуемые продукты:
```sql
SELECT name, price 
FROM products 
WHERE is_featured = true;
```

### Посмотреть все филиалы по городам:
```sql
SELECT city, COUNT(*) as count 
FROM branches 
GROUP BY city 
ORDER BY count DESC;
```

### Посмотреть статистику:
```sql
SELECT 
  (SELECT COUNT(*) FROM product_categories) as categories,
  (SELECT COUNT(*) FROM products) as products,
  (SELECT COUNT(*) FROM branches) as branches,
  (SELECT COUNT(*) FROM products WHERE is_featured = true) as featured_products;
```

## 🎨 Customization

Если вы хотите изменить:
- **Цены** - отредактируйте значения `price` в SQL скрипте
- **Описания** - измените текст в поле `description`
- **Рекомендуемые** - измените `is_featured` на `true/false`
- **Наличие** - измените `in_stock` на `true/false`

## ⚠️ Важные заметки

1. **Скрипт безопасен** - использует `ON CONFLICT DO UPDATE`, поэтому можно запускать несколько раз
2. **Категории сначала** - они создаются первыми, так как продукты ссылаются на них
3. **Изображения** - все продукты используют placeholder `/images/products/productlogo.png`
4. **Email-адреса** - обновите email-адреса филиалов на реальные

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что схема базы создана (`supabase-schema-fix.sql`)
2. Проверьте права доступа в Supabase (RLS policies)
3. Посмотрите логи в Supabase → Logs

---

**Теперь ваш сайт готов к работе!** 🎉



