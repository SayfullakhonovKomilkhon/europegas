# 🚀 Настройка Supabase для EuropeGAS

## Шаг 1: Запуск SQL схемы

Откройте Supabase Dashboard → SQL Editor и выполните содержимое файла:
```
supabase-schema-fix.sql
```

## Шаг 2: Создание администратора

### Вариант A: Через Supabase Dashboard (рекомендуется)

1. Откройте **Authentication → Users** в Supabase Dashboard
2. Нажмите **Add user → Create new user**
3. Введите:
   - Email: `admin@europegas.uz`
   - Password: `ваш_пароль`
   - Auto Confirm: ✅ Включено
4. После создания пользователя, откройте **SQL Editor** и выполните:

```sql
-- Назначить роль superadmin
UPDATE public.user_profiles 
SET role = 'superadmin' 
WHERE email = 'admin@europegas.uz';
```

### Вариант B: Через SQL (если триггер работает)

```sql
-- Просто обновите роль существующего пользователя
UPDATE public.user_profiles 
SET role = 'superadmin' 
WHERE email = 'ваш-email@example.com';
```

## Шаг 3: Настройка .env.local

Создайте файл `.env.local` в корне проекта:

```env
REACT_APP_SUPABASE_URL=https://ваш-проект.supabase.co
REACT_APP_SUPABASE_ANON_KEY=ваш_anon_key
```

Найти эти значения можно в Supabase Dashboard → Settings → API

## Шаг 4: Перезапуск приложения

```bash
npm start
```

## Проверка

1. Откройте http://localhost:3000/admin/login
2. Войдите с email и паролем администратора
3. Вы должны увидеть админ-панель

## Устранение проблем

### Ошибка "Доступ запрещен"
- Убедитесь что пользователь имеет роль 'admin' или 'superadmin' в таблице `user_profiles`

### Ошибка при регистрации
- Проверьте что триггер `on_auth_user_created` существует в базе данных
- Можно проверить: Database → Triggers

### Проверка данных
```sql
-- Посмотреть всех пользователей и их роли
SELECT id, email, role, created_at 
FROM public.user_profiles 
ORDER BY created_at DESC;

-- Посмотреть все таблицы
SELECT tablename FROM pg_tables WHERE schemaname = 'public';
```

## Структура таблиц

| Таблица | Описание |
|---------|----------|
| user_profiles | Профили пользователей с ролями |
| products | Продукты магазина |
| product_categories | Категории продуктов |
| branches | Филиалы магазина |
| orders | Заказы клиентов |
| order_items | Позиции в заказах |
| testimonials | Отзывы клиентов |
| contact_messages | Сообщения с формы контактов |
| cars | Автомобили пользователей |
| audit_logs | Логи действий |

## Роли пользователей

- **customer** - обычный покупатель (по умолчанию)
- **admin** - администратор с доступом к админ-панели
- **superadmin** - суперадминистратор с полным доступом
